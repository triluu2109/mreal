"use server";

export async function createProperty() {
  return { success: false, error: "Use createSell/createRent instead." };
}

export async function updateProperty() {
  return { success: false, error: "Use updateSell/updateRent instead." };
}

export async function deleteProperty() {
  return { success: false, error: "Use deleteSell/deleteRent instead." };
}
